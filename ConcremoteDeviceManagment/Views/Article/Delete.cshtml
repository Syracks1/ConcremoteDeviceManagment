@model ConcremoteDeviceManagment.Models.Pricelist

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Pricelist</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayName("BAS artikelnummer")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.bas_art_nr)
        </dd>
        <dt>
            @Html.DisplayName("Prijs")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Price)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

    <div class="form-actions no-color">
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <button onclick="location.href='@Url.Action("Index", "Article")';return false; " class="btn btn-primary">Back to list</button>
    </div>
    }
</div>
