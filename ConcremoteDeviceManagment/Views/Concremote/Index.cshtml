@model IEnumerable<ConcremoteDeviceManagment.Models.DeviceStatus_ExtraInfo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Device Naam")
        </th>
        <th>
            @Html.DisplayName("In Gebruik")
        </th>
        <th>
            @Html.DisplayName("Config Versie")
        </th>
        <th>
            @Html.DisplayName("Config Datum")
        </th>
        <th>
            @Html.DisplayName("Status")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceStatus.DeviceConfig.DeviceType.name)
        </td>
        
        <td>
            @Html.DisplayFor(modelItem => item.DeviceStatus.ConcremoteDevice.Active)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceStatus.DeviceConfig.VersionNr)
        </td>
        <td>
          @Html.DisplayFor(modelItem => item.DeviceStatus.DeviceConfig.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceStatus.Device_Statustypes.name)
        </td>
        <td>
            <button onclick="location.href='@Url.Action("Edit", "Concremote", new { id=item.id})';return false;" class="btn btn-primary">Edit</button>
            <button onclick="location.href='@Url.Action("Details", "Concremote", new { id=item.id})';return false;" class="btn btn-info">Details</button>
            <button onclick="location.href='@Url.Action("Delete", "Concremote", new { id=item.id})';return false;" class="btn btn-danger">Delete</button>
        </td>
    </tr>
}

</table>
