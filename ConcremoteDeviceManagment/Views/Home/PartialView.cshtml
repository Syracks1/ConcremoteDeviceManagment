@model IEnumerable<ConcremoteDeviceManagment.Models.DeviceConfig>

<p>
  @*  @Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DeviceType.device_type_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DeviceType.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pricelist.price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.assembly_order)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price_id)
        </th>
        <th></th>
    </tr>

@*@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceType.device_type_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceType.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pricelist.price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.assembly_order)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price_id)
        </td>
    </tr>
}*@

</table>
    @*<script>
    $("#SelectedDevice").change( function (event) {
          var device_type_id = $(this).val();
            $.ajax({
                url: "@Url.Action("PartialView", "Home")",
                data: { id : device_type_id },
                type: "Get",
                dataType: "html",
                success: function (data) {
                //Whatever result you have got from your controller with html partial view replace with a specific html.
                $("#divPartialView").html( data ); // HTML DOM replace
                }
                });
                });
    </script>*@