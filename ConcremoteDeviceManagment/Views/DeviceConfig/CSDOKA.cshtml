@model IEnumerable<ConcremoteDeviceManagment.Models.Stock>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.bas_art_nr)
        </th>
        <th>
           
            @*@Html.DisplayNameFor(model => model.)*@
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.bas_art_nr)
        </td>
        <td>
          
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.device_type_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.device_type_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.device_type_id })*@
        </td>
    </tr>
}

</table>
